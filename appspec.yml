version: 0.0
Resources:
  - TargetService:
      Type: AWS::ECS::SERVICE
      Properties:
        TaskDefinition: "<TASK_DEFINITION>"
        LoadBalancerInfo:
          ContainerName: "ctn-cicd-hdon-cont"
          ContainerPort: 80
